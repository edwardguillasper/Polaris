<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.google.mlkit.vision.demo"
    android:installLocation="auto">
    <!-- CameraX libraries require minSdkVersion 21, while this quickstart app
    supports low to 19. Needs to use overrideLibrary to make the merger tool
    ignore this conflict and import the libraries while keeping the app's lower
    minSdkVersion value. In code, will check SDK version, before calling CameraX
    APIs. -->
    <uses-sdk
        tools:overrideLibrary="
          androidx.camera.camera2, androidx.camera.core,
          androidx.camera.view, androidx.camera.lifecycle,
          com.google.mlkit.vision.segmentation.subject" />

    <uses-feature android:name="android.hardware.camera"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:name=".PolarisApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.DayNight">

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>

        <!-- Optional: Add it to automatically download ML model to device after
          your app is installed.-->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode,face,ocr,ocr_chinese,ocr_devanagari,ocr_japanese,ocr_korean,ica,custom_ica,subject_segment"/>

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".GetStartedActivity"
            android:exported="false"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".java.ChooserActivity"
            android:exported="false"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".java.CameraXLivePreviewActivity"
            android:exported="false"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".java.TextToSpeechLivePreviewActivity"
            android:exported="false"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".java.LoginActivity"
            android:exported="false"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".java.SignUpActivity"
            android:exported="false"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".java.ActivityLogActivity"
            android:exported="false"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".java.UserGuideActivity"
            android:exported="false"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".java.FaqActivity"
            android:exported="false"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".java.DevicePermissionsActivity"
            android:exported="false"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".java.NotificationsActivity"
            android:exported="false"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".java.ManageAccountActivity"
            android:exported="false"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".preference.SettingsActivity"
            android:exported="false"/>

    </application>
    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <package android:name="com.google.android.apps.maps" />
    </queries>

</manifest>
